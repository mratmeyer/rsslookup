{
  "name": "rsslookup-worker",
  "version": "1.0.0",
  "description": "RSS Lookup Workers Backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "mratmeyer",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "htmlparser2": "^8.0.1"
  }
}
